struct Foo {
    bar: Baz,
}

fn test() -> Hello {
    let foo = bar;
    let a = b();
    c + b + a + c * a + a() + b() * c(a((a + a)));
}

fn test() {
    let a = 1;
}

fn main() {
    let a = 0;
    let b = 1;
    let c = a + b;
    let d = a[0.0 + 1.0];
    let e = a.b;

    let foo = Foo {
        bar: 1.0,
        baz: 1 + 2 + 3 + 4,
    };

    gl_frag_color = vec4(1.0, 1.0, 1.0, 1.0);
}

fn foo() {
    let x = y;
}

fn bar() {
    let x = 10;
    10;
}
