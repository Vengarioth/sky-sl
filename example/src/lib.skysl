struct Foo {
    bar: Baz,
}

fn main() {
    let a = 0;
    let b = 1;
    let c = a + b;
    let d = a[0.0 + 1.0];
    let e = a.b;

    let foo = Foo {
        bar: 1.0,
        baz: 1 + 2 + 3 + 4,
    };

    gl_frag_color = vec4(1.0, 1.0, 1.0, 1.0);
}
